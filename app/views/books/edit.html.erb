<h1>編集画面</h1>
<%= form_for(@book) do |f| %>
    <h4>タイトル</h4>
    <%= f.text_field :title %>
    <h4>本文</h4>
    <%= f.text_area :body %>
    <%= f.submit '保存' %>
<% end %>

<%= link_to"Show", book_path(@book) %><p>|</p><%= link_to"Back", book_path %>